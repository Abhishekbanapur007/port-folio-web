<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Abhishek Banapur | Portfolio</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<style>
  :root {
    --bg: #050a12;
    --bg2: #080f1c;
    --bg3: #0b1526;
    --glass: rgba(255,255,255,0.035);
    --glass-border: rgba(255,255,255,0.08);
    --accent: #00d4ff;
    --accent2: #7c3aff;
    --accent3: #00ffb3;
    --text: #e8edf5;
    --text-muted: #6b7a99;
    --glow: 0 0 30px rgba(0,212,255,0.25);
    --glow2: 0 0 30px rgba(124,58,255,0.25);
    --radius: 16px;
    --font-display: 'Syne', sans-serif;
    --font-body: 'DM Sans', sans-serif;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font-body);
    overflow-x: hidden;
    line-height: 1.6;
  }

  /* ─── SCROLLBAR ─── */
  ::-webkit-scrollbar { width: 4px; }
  ::-webkit-scrollbar-track { background: var(--bg); }
  ::-webkit-scrollbar-thumb { background: var(--accent2); border-radius: 2px; }

  /* ─── CANVAS BG ─── */
  #canvas-bg {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    pointer-events: none;
    z-index: 0;
    opacity: 0.4;
  }

  /* ─── NAV ─── */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 1000;
    padding: 18px 40px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(5,10,18,0.7);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--glass-border);
    transition: all 0.3s;
  }

  .nav-logo {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.3rem;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    letter-spacing: -0.5px;
  }

  .nav-links {
    display: flex;
    gap: 32px;
    list-style: none;
  }

  .nav-links a {
    color: var(--text-muted);
    text-decoration: none;
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    transition: color 0.3s;
    position: relative;
  }

  .nav-links a::after {
    content: '';
    position: absolute;
    bottom: -4px; left: 0;
    width: 0; height: 1px;
    background: var(--accent);
    transition: width 0.3s;
  }

  .nav-links a:hover { color: var(--accent); }
  .nav-links a:hover::after { width: 100%; }

  .nav-hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    cursor: pointer;
    z-index: 1001;
  }

  .nav-hamburger span {
    display: block;
    width: 24px;
    height: 2px;
    background: var(--accent);
    transition: all 0.3s;
    border-radius: 2px;
  }

  .mobile-menu {
    display: none;
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: rgba(5,10,18,0.97);
    backdrop-filter: blur(30px);
    z-index: 999;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 40px;
  }

  .mobile-menu.open { display: flex; }

  .mobile-menu a {
    color: var(--text);
    text-decoration: none;
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 700;
    transition: color 0.3s;
  }

  .mobile-menu a:hover { color: var(--accent); }

  /* ─── SECTIONS ─── */
  section {
    position: relative;
    z-index: 1;
    padding: 100px 40px;
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-tag {
    display: inline-block;
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--accent);
    border: 1px solid rgba(0,212,255,0.3);
    border-radius: 100px;
    padding: 5px 14px;
    margin-bottom: 20px;
    background: rgba(0,212,255,0.05);
  }

  .section-title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 16px;
  }

  .gradient-text {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* ─── HERO ─── */
  #hero {
    min-height: 100vh;
    max-width: 100%;
    padding: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .hero-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
    animation: orbFloat 8s ease-in-out infinite;
  }

  .hero-orb-1 {
    width: 500px; height: 500px;
    background: radial-gradient(circle, rgba(0,212,255,0.15), transparent);
    top: -100px; left: -150px;
    animation-delay: 0s;
  }

  .hero-orb-2 {
    width: 400px; height: 400px;
    background: radial-gradient(circle, rgba(124,58,255,0.2), transparent);
    bottom: -80px; right: -100px;
    animation-delay: -4s;
  }

  .hero-orb-3 {
    width: 300px; height: 300px;
    background: radial-gradient(circle, rgba(0,255,179,0.1), transparent);
    top: 50%; right: 10%;
    animation-delay: -2s;
  }

  @keyframes orbFloat {
    0%, 100% { transform: translateY(0px) scale(1); }
    50% { transform: translateY(-30px) scale(1.05); }
  }

  .hero-content {
    position: relative;
    z-index: 2;
    padding: 40px 20px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(0,212,255,0.08);
    border: 1px solid rgba(0,212,255,0.2);
    border-radius: 100px;
    padding: 8px 18px;
    font-size: 0.8rem;
    color: var(--accent);
    margin-bottom: 32px;
    animation: fadeUp 0.8s ease forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-badge-dot {
    width: 6px; height: 6px;
    background: var(--accent3);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.5); }
  }

  /* PHOTO WRAPPER */
  .hero-photo-wrapper {
    position: relative;
    display: inline-block;
    margin-bottom: 32px;
    animation: fadeUp 0.8s 0.1s ease forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-photo {
    width: 130px;
    height: 130px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--bg3), var(--bg2));
    border: 2px solid transparent;
    background-clip: padding-box;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    box-shadow: 0 0 0 3px rgba(0,212,255,0.3), 0 0 40px rgba(0,212,255,0.2), 0 0 80px rgba(124,58,255,0.1);
  }

  .hero-photo-ring {
    position: absolute;
    inset: -5px;
    border-radius: 50%;
    background: conic-gradient(from 0deg, var(--accent), var(--accent2), var(--accent3), var(--accent));
    animation: spinRing 4s linear infinite;
    z-index: -1;
  }

  @keyframes spinRing {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .hero-photo-inner {
    width: 124px;
    height: 124px;
    border-radius: 50%;
    background: var(--bg2);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  .hero-photo-silhouette {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    overflow: hidden;
    background: linear-gradient(180deg, #0d1a30 0%, #162035 100%);
  }

  .hero-photo-silhouette svg {
    width: 85%;
    fill: rgba(0,212,255,0.4);
    filter: drop-shadow(0 0 8px rgba(0,212,255,0.5));
  }

  .hero-name {
    font-family: var(--font-display);
    font-size: clamp(2.8rem, 7vw, 5.5rem);
    font-weight: 800;
    line-height: 1;
    letter-spacing: -2px;
    margin-bottom: 16px;
    animation: fadeUp 0.8s 0.2s ease forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-name span {
    display: block;
    background: linear-gradient(135deg, #fff 30%, rgba(255,255,255,0.5));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .hero-name .name-accent {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .hero-sub {
    font-size: clamp(0.95rem, 2vw, 1.15rem);
    color: var(--text-muted);
    margin-bottom: 20px;
    font-weight: 300;
    letter-spacing: 0.3px;
    animation: fadeUp 0.8s 0.35s ease forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-desc {
    font-size: clamp(0.85rem, 1.5vw, 1rem);
    color: var(--text-muted);
    max-width: 560px;
    margin: 0 auto 40px;
    line-height: 1.8;
    animation: fadeUp 0.8s 0.5s ease forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  .hero-btns {
    display: flex;
    gap: 14px;
    justify-content: center;
    flex-wrap: wrap;
    animation: fadeUp 0.8s 0.65s ease forwards;
    opacity: 0;
    transform: translateY(20px);
  }

  @keyframes fadeUp {
    to { opacity: 1; transform: translateY(0); }
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 13px 26px;
    border-radius: 100px;
    font-weight: 600;
    font-size: 0.88rem;
    text-decoration: none;
    transition: all 0.3s;
    cursor: pointer;
    border: none;
    font-family: var(--font-body);
    letter-spacing: 0.3px;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    color: #fff;
    box-shadow: 0 0 25px rgba(0,212,255,0.3);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 35px rgba(0,212,255,0.5);
  }

  .btn-outline {
    background: transparent;
    color: var(--text);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(10px);
  }

  .btn-outline:hover {
    border-color: var(--accent);
    color: var(--accent);
    transform: translateY(-2px);
    background: rgba(0,212,255,0.05);
  }

  .btn-ghost {
    background: rgba(0,255,179,0.08);
    color: var(--accent3);
    border: 1px solid rgba(0,255,179,0.2);
  }

  .btn-ghost:hover {
    background: rgba(0,255,179,0.15);
    transform: translateY(-2px);
    box-shadow: 0 0 20px rgba(0,255,179,0.2);
  }

  .scroll-hint {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    color: var(--text-muted);
    font-size: 0.7rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    animation: fadeUp 1s 1s ease forwards;
    opacity: 0;
  }

  .scroll-line {
    width: 1px;
    height: 50px;
    background: linear-gradient(to bottom, var(--accent), transparent);
    animation: scrollLine 2s ease-in-out infinite;
  }

  @keyframes scrollLine {
    0%, 100% { opacity: 0.3; transform: scaleY(0.5); }
    50% { opacity: 1; transform: scaleY(1); }
  }

  /* ─── DIVIDER ─── */
  .divider {
    width: 100%;
    height: 1px;
    background: linear-gradient(to right, transparent, var(--glass-border), transparent);
    position: relative;
    z-index: 1;
  }

  /* ─── GLASS CARD ─── */
  .glass-card {
    background: var(--glass);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius);
    backdrop-filter: blur(20px);
    transition: all 0.4s;
  }

  .glass-card:hover {
    border-color: rgba(0,212,255,0.2);
    transform: translateY(-4px);
    box-shadow: var(--glow);
  }

  /* ─── ABOUT ─── */
  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 60px;
    align-items: center;
    margin-top: 60px;
  }

  .about-img-wrapper {
    position: relative;
  }

  .about-img-bg {
    width: 100%;
    aspect-ratio: 3/4;
    border-radius: 20px;
    background: linear-gradient(135deg, var(--bg3), var(--bg2));
    border: 1px solid var(--glass-border);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    position: relative;
  }

  .about-img-bg::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(0,212,255,0.05), rgba(124,58,255,0.05));
  }

  .about-silhouette {
    width: 70%;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    position: relative;
    z-index: 1;
  }

  .about-silhouette svg {
    width: 100%;
    fill: rgba(0,212,255,0.25);
    filter: drop-shadow(0 0 15px rgba(0,212,255,0.3));
  }

  .about-stat-cards {
    position: absolute;
    right: -20px;
    bottom: 40px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .stat-card {
    background: rgba(5,10,18,0.9);
    border: 1px solid var(--glass-border);
    border-radius: 12px;
    padding: 12px 16px;
    backdrop-filter: blur(20px);
    display: flex;
    align-items: center;
    gap: 12px;
    width: 160px;
  }

  .stat-icon {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    flex-shrink: 0;
  }

  .stat-icon.cyan { background: rgba(0,212,255,0.15); color: var(--accent); }
  .stat-icon.purple { background: rgba(124,58,255,0.15); color: var(--accent2); }
  .stat-icon.green { background: rgba(0,255,179,0.15); color: var(--accent3); }

  .stat-info { line-height: 1.3; }
  .stat-num { font-family: var(--font-display); font-weight: 700; font-size: 1.1rem; color: var(--text); }
  .stat-label { font-size: 0.68rem; color: var(--text-muted); }

  .about-content p {
    color: var(--text-muted);
    font-size: 1rem;
    margin-bottom: 16px;
    line-height: 1.85;
  }

  .about-traits {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 24px;
  }

  .trait-tag {
    background: rgba(255,255,255,0.04);
    border: 1px solid var(--glass-border);
    border-radius: 100px;
    padding: 6px 14px;
    font-size: 0.78rem;
    color: var(--text-muted);
    transition: all 0.3s;
  }

  .trait-tag:hover {
    border-color: var(--accent);
    color: var(--accent);
    background: rgba(0,212,255,0.05);
  }

  /* ─── EDUCATION ─── */
  .edu-timeline {
    position: relative;
    margin-top: 60px;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .edu-timeline::before {
    content: '';
    position: absolute;
    left: 24px;
    top: 0; bottom: 0;
    width: 1px;
    background: linear-gradient(to bottom, var(--accent), var(--accent2), transparent);
  }

  .edu-item {
    display: flex;
    gap: 24px;
    align-items: flex-start;
    padding-left: 8px;
  }

  .edu-dot {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    flex-shrink: 0;
    position: relative;
    z-index: 1;
    margin-top: 4px;
  }

  .edu-dot.active {
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    color: #fff;
    box-shadow: 0 0 20px rgba(0,212,255,0.4);
  }

  .edu-dot.done {
    background: rgba(255,255,255,0.05);
    border: 2px solid var(--glass-border);
    color: var(--text-muted);
  }

  .edu-card {
    flex: 1;
    padding: 24px 28px;
    border-radius: 16px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    transition: all 0.4s;
  }

  .edu-card:hover {
    border-color: rgba(0,212,255,0.2);
    transform: translateX(6px);
    box-shadow: var(--glow);
  }

  .edu-degree {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.1rem;
    margin-bottom: 4px;
    color: var(--text);
  }

  .edu-school {
    color: var(--accent);
    font-size: 0.9rem;
    margin-bottom: 8px;
  }

  .edu-meta {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  .edu-badge {
    font-size: 0.72rem;
    padding: 4px 10px;
    border-radius: 100px;
    font-weight: 600;
  }

  .edu-badge.active { background: rgba(0,255,179,0.1); color: var(--accent3); border: 1px solid rgba(0,255,179,0.2); }
  .edu-badge.score { background: rgba(0,212,255,0.1); color: var(--accent); border: 1px solid rgba(0,212,255,0.2); }

  /* ─── PROJECTS ─── */
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
    margin-top: 60px;
  }

  .project-card {
    padding: 28px;
    border-radius: 20px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    cursor: pointer;
    transition: all 0.4s;
    position: relative;
    overflow: hidden;
  }

  .project-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s;
  }

  .project-card:hover::before { transform: scaleX(1); }

  .project-card:hover {
    border-color: rgba(0,212,255,0.2);
    transform: translateY(-8px);
    box-shadow: 0 20px 60px rgba(0,0,0,0.4), var(--glow);
  }

  .project-icon {
    width: 54px;
    height: 54px;
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.4rem;
    margin-bottom: 20px;
    transition: all 0.3s;
  }

  .project-card:hover .project-icon {
    transform: scale(1.1) rotate(-5deg);
  }

  .pi-1 { background: rgba(0,212,255,0.1); color: var(--accent); }
  .pi-2 { background: rgba(124,58,255,0.1); color: var(--accent2); }
  .pi-3 { background: rgba(0,255,179,0.1); color: var(--accent3); }
  .pi-4 { background: rgba(255,165,0,0.1); color: #ffa500; }
  .pi-5 { background: rgba(255,80,120,0.1); color: #ff5078; }
  .pi-6 { background: rgba(80,200,255,0.1); color: #50c8ff; }

  .project-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1.05rem;
    margin-bottom: 10px;
  }

  .project-desc {
    color: var(--text-muted);
    font-size: 0.85rem;
    line-height: 1.7;
    margin-bottom: 20px;
  }

  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .ptag {
    font-size: 0.68rem;
    padding: 4px 10px;
    border-radius: 100px;
    background: rgba(255,255,255,0.04);
    border: 1px solid var(--glass-border);
    color: var(--text-muted);
  }

  .project-arrow {
    position: absolute;
    top: 24px;
    right: 24px;
    color: var(--text-muted);
    transition: all 0.3s;
    opacity: 0;
  }

  .project-card:hover .project-arrow {
    opacity: 1;
    color: var(--accent);
    transform: translate(2px, -2px);
  }

  /* ─── SKILLS ─── */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 16px;
    margin-top: 60px;
  }

  .skill-item {
    padding: 20px 22px;
    border-radius: 14px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    transition: all 0.3s;
  }

  .skill-item:hover {
    border-color: rgba(0,212,255,0.2);
    background: rgba(0,212,255,0.04);
  }

  .skill-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 12px;
  }

  .skill-name {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 500;
    font-size: 0.9rem;
  }

  .skill-icon {
    width: 28px;
    height: 28px;
    border-radius: 7px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.85rem;
  }

  .skill-pct {
    font-size: 0.78rem;
    color: var(--text-muted);
    font-weight: 600;
  }

  .skill-bar {
    height: 4px;
    background: rgba(255,255,255,0.06);
    border-radius: 4px;
    overflow: hidden;
  }

  .skill-fill {
    height: 100%;
    border-radius: 4px;
    position: relative;
    transition: width 1.5s cubic-bezier(0.16, 1, 0.3, 1);
    width: 0;
  }

  .skill-fill::after {
    content: '';
    position: absolute;
    right: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 8px;
    height: 8px;
    border-radius: 50%;
  }

  .sf-cyan { background: linear-gradient(to right, var(--accent), rgba(0,212,255,0.5)); }
  .sf-cyan::after { background: var(--accent); box-shadow: 0 0 8px var(--accent); }
  .sf-purple { background: linear-gradient(to right, var(--accent2), rgba(124,58,255,0.5)); }
  .sf-purple::after { background: var(--accent2); box-shadow: 0 0 8px var(--accent2); }
  .sf-green { background: linear-gradient(to right, var(--accent3), rgba(0,255,179,0.5)); }
  .sf-green::after { background: var(--accent3); box-shadow: 0 0 8px var(--accent3); }
  .sf-orange { background: linear-gradient(to right, #ffa500, rgba(255,165,0,0.5)); }
  .sf-orange::after { background: #ffa500; box-shadow: 0 0 8px #ffa500; }

  /* ─── ACHIEVEMENTS ─── */
  .ach-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 20px;
    margin-top: 60px;
  }

  .ach-card {
    padding: 28px;
    border-radius: 18px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    transition: all 0.4s;
    position: relative;
    overflow: hidden;
  }

  .ach-card::before {
    content: '';
    position: absolute;
    bottom: 0; right: 0;
    width: 100px; height: 100px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(0,212,255,0.06), transparent);
    transform: translate(30px, 30px);
  }

  .ach-card:hover {
    border-color: rgba(0,212,255,0.2);
    transform: translateY(-4px);
    box-shadow: var(--glow);
  }

  .ach-icon {
    font-size: 1.8rem;
    margin-bottom: 16px;
  }

  .ach-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 1rem;
    margin-bottom: 8px;
  }

  .ach-text {
    color: var(--text-muted);
    font-size: 0.85rem;
    line-height: 1.7;
  }

  /* ─── CONTACT ─── */
  .contact-layout {
    display: grid;
    grid-template-columns: 1fr 1.3fr;
    gap: 60px;
    margin-top: 60px;
    align-items: start;
  }

  .contact-info h3 {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 16px;
  }

  .contact-info p {
    color: var(--text-muted);
    font-size: 0.9rem;
    line-height: 1.8;
    margin-bottom: 32px;
  }

  .contact-links {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .contact-link {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 18px;
    border-radius: 12px;
    background: var(--glass);
    border: 1px solid var(--glass-border);
    text-decoration: none;
    color: var(--text);
    transition: all 0.3s;
    font-size: 0.88rem;
  }

  .contact-link:hover {
    border-color: var(--accent);
    color: var(--accent);
    transform: translateX(4px);
    background: rgba(0,212,255,0.04);
  }

  .contact-link-icon {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    flex-shrink: 0;
  }

  .cli-github { background: rgba(255,255,255,0.08); color: var(--text); }
  .cli-linkedin { background: rgba(0,119,181,0.15); color: #0077b5; }
  .cli-email { background: rgba(0,212,255,0.1); color: var(--accent); }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .form-group label {
    font-size: 0.78rem;
    font-weight: 600;
    color: var(--text-muted);
    letter-spacing: 0.5px;
    text-transform: uppercase;
  }

  .form-group input,
  .form-group textarea {
    background: rgba(255,255,255,0.04);
    border: 1px solid var(--glass-border);
    border-radius: 10px;
    padding: 13px 16px;
    color: var(--text);
    font-family: var(--font-body);
    font-size: 0.9rem;
    transition: all 0.3s;
    outline: none;
    resize: none;
  }

  .form-group input:focus,
  .form-group textarea:focus {
    border-color: var(--accent);
    background: rgba(0,212,255,0.04);
    box-shadow: 0 0 0 3px rgba(0,212,255,0.08);
  }

  .form-group textarea { height: 120px; }

  .form-submit {
    align-self: flex-start;
  }

  /* ─── FOOTER ─── */
  footer {
    position: relative;
    z-index: 1;
    text-align: center;
    padding: 40px 20px;
    border-top: 1px solid var(--glass-border);
  }

  .footer-logo {
    font-family: var(--font-display);
    font-size: 1.4rem;
    font-weight: 800;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: 16px;
  }

  .footer-text {
    color: var(--text-muted);
    font-size: 0.82rem;
  }

  .footer-heart {
    color: #ff5078;
    animation: heartbeat 1.5s infinite;
    display: inline-block;
  }

  @keyframes heartbeat {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.3); }
  }

  /* ─── SECTION REVEAL ─── */
  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ─── MOBILE ─── */
  @media (max-width: 768px) {
    nav { padding: 16px 20px; }
    .nav-links { display: none; }
    .nav-hamburger { display: flex; }
    section { padding: 70px 20px; }
    .about-grid { grid-template-columns: 1fr; gap: 40px; }
    .about-stat-cards { position: static; flex-direction: row; flex-wrap: wrap; }
    .stat-card { width: auto; flex: 1 1 140px; }
    .contact-layout { grid-template-columns: 1fr; gap: 40px; }
    .hero-name { letter-spacing: -1px; }
    .edu-timeline::before { left: 16px; }
    .edu-item { padding-left: 0; }
  }

  @media (max-width: 480px) {
    .hero-btns { flex-direction: column; align-items: center; }
    .btn { width: 100%; justify-content: center; max-width: 260px; }
  }
</style>
</head>
<body>

<canvas id="canvas-bg"></canvas>

<!-- ─── NAV ─── -->
<nav id="navbar">
  <div class="nav-logo">AB.</div>
  <ul class="nav-links">
    <li><a href="#about">About</a></li>
    <li><a href="#education">Education</a></li>
    <li><a href="#projects">Projects</a></li>
    <li><a href="#skills">Skills</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <div class="nav-hamburger" id="hamburger" onclick="toggleMenu()">
    <span></span><span></span><span></span>
  </div>
</nav>

<div class="mobile-menu" id="mobileMenu">
  <a href="#about" onclick="toggleMenu()">About</a>
  <a href="#education" onclick="toggleMenu()">Education</a>
  <a href="#projects" onclick="toggleMenu()">Projects</a>
  <a href="#skills" onclick="toggleMenu()">Skills</a>
  <a href="#contact" onclick="toggleMenu()">Contact</a>
</div>

<!-- ─── HERO ─── -->
<div id="hero">
  <div class="hero-orb hero-orb-1"></div>
  <div class="hero-orb hero-orb-2"></div>
  <div class="hero-orb hero-orb-3"></div>

  <div class="hero-content">
    <div class="hero-badge">
      <div class="hero-badge-dot"></div>
      Available for Opportunities
    </div>

    <!-- Photo -->
    <div class="hero-photo-wrapper">
      <div class="hero-photo-ring"></div>
      <div class="hero-photo">
        <div class="hero-photo-inner">
          <div class="hero-photo-silhouette">
            <!-- Man silhouette SVG -->
            <svg viewBox="0 0 100 120" xmlns="http://www.w3.org/2000/svg">
              <circle cx="50" cy="28" r="18"/>
              <path d="M20 120 C20 80 30 65 50 65 C70 65 80 80 80 120Z"/>
            </svg>
          </div>
        </div>
      </div>
    </div>

    <div class="hero-name">
      <span>Abhishek</span>
      <span class="name-accent">Banapur</span>
    </div>
    <p class="hero-sub">B.Tech Computer Science Student &nbsp;|&nbsp; Future AI &amp; Software Developer</p>
    <p class="hero-desc">Passionate about technology, AI, software development, and building projects that help society. Turning ideas into impactful digital experiences.</p>

    <div class="hero-btns">
      <a href="#projects" class="btn btn-primary"><i class="fas fa-rocket"></i> View Projects</a>
      <a href="#contact" class="btn btn-outline"><i class="fas fa-paper-plane"></i> Contact Me</a>
      <a href="#" class="btn btn-ghost" onclick="alert('Resume download coming soon!')"><i class="fas fa-download"></i> Download Resume</a>
    </div>
  </div>

  <div class="scroll-hint">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</div>

<div class="divider"></div>

<!-- ─── ABOUT ─── -->
<section id="about">
  <div class="reveal">
    <div class="section-tag">Who I Am</div>
    <h2 class="section-title">About <span class="gradient-text">Me</span></h2>
  </div>

  <div class="about-grid reveal">
    <div class="about-img-wrapper">
      <div class="about-img-bg">
        <div class="about-silhouette">
          <svg viewBox="0 0 100 140" xmlns="http://www.w3.org/2000/svg">
            <circle cx="50" cy="30" r="22"/>
            <path d="M15 140 C15 90 28 72 50 72 C72 72 85 90 85 140Z"/>
          </svg>
        </div>
      </div>
      <div class="about-stat-cards">
        <div class="stat-card">
          <div class="stat-icon cyan"><i class="fas fa-code"></i></div>
          <div class="stat-info">
            <div class="stat-num">10+</div>
            <div class="stat-label">Projects Built</div>
          </div>
        </div>
        <div class="stat-card">
          <div class="stat-icon purple"><i class="fas fa-brain"></i></div>
          <div class="stat-info">
            <div class="stat-num">AI</div>
            <div class="stat-label">Focused Dev</div>
          </div>
        </div>
        <div class="stat-card">
          <div class="stat-icon green"><i class="fas fa-star"></i></div>
          <div class="stat-info">
            <div class="stat-num">89%</div>
            <div class="stat-label">PUC Score</div>
          </div>
        </div>
      </div>
    </div>

    <div class="about-content">
      <p>Currently pursuing <strong>B.Tech in Computer Science</strong> at R.V. College of Engineering, Bangalore — one of India's most prestigious engineering institutions.</p>
      <p>I'm deeply interested in <strong>Artificial Intelligence, Web Development</strong>, and futuristic technologies that can reshape the world. I believe technology is the most powerful tool humanity has for solving real problems.</p>
      <p>Working on impactful projects focused on <strong>improving society using technology</strong> — from AI applications to smart systems. I have a strong drive to build products that are not just technically sound, but genuinely useful to people.</p>
      <p>My vision: become an <strong>AI Engineer and Software Developer</strong> who builds meaningful products at the intersection of innovation and social impact.</p>

      <div class="about-traits">
        <span class="trait-tag">🤖 AI Enthusiast</span>
        <span class="trait-tag">💻 Web Developer</span>
        <span class="trait-tag">🚀 Innovation-driven</span>
        <span class="trait-tag">🌏 Society-focused</span>
        <span class="trait-tag">🔬 Problem Solver</span>
        <span class="trait-tag">⚡ Fast Learner</span>
        <span class="trait-tag">🌱 Startup Minded</span>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ─── EDUCATION ─── -->
<section id="education">
  <div class="reveal">
    <div class="section-tag">Academic Journey</div>
    <h2 class="section-title">My <span class="gradient-text">Education</span></h2>
  </div>

  <div class="edu-timeline">
    <!-- RVCE -->
    <div class="edu-item reveal">
      <div class="edu-dot active"><i class="fas fa-graduation-cap"></i></div>
      <div class="edu-card">
        <div class="edu-degree">B.Tech — Computer Science &amp; Engineering</div>
        <div class="edu-school">R.V. College of Engineering, Bangalore</div>
        <div class="edu-meta">
          <span class="edu-badge active">✦ Presently Pursuing</span>
        </div>
      </div>
    </div>

    <!-- PUC -->
    <div class="edu-item reveal">
      <div class="edu-dot done"><i class="fas fa-book"></i></div>
      <div class="edu-card">
        <div class="edu-degree">PUC — 2nd Year (Science)</div>
        <div class="edu-school">SIR M V PU College</div>
        <div class="edu-meta">
          <span class="edu-badge score">Score: 89%</span>
        </div>
      </div>
    </div>

    <!-- ICSE -->
    <div class="edu-item reveal">
      <div class="edu-dot done"><i class="fas fa-school"></i></div>
      <div class="edu-card">
        <div class="edu-degree">Schooling — ICSE Board</div>
        <div class="edu-school">Kallukamba Village, Bellary District</div>
        <div class="edu-meta">
          <span class="edu-badge score">Score: 80%</span>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ─── PROJECTS ─── -->
<section id="projects">
  <div class="reveal">
    <div class="section-tag">What I Build</div>
    <h2 class="section-title">My <span class="gradient-text">Projects</span></h2>
    <p style="color:var(--text-muted);max-width:560px;margin-top:12px;font-size:0.95rem;">Building the future one project at a time — from AI solutions to smart web applications.</p>
  </div>

  <div class="projects-grid">
    <div class="project-card reveal">
      <div class="project-icon pi-1"><i class="fas fa-brain"></i></div>
      <i class="fas fa-arrow-up-right-from-square project-arrow"></i>
      <div class="project-title">AI Smart Assistant</div>
      <div class="project-desc">An intelligent conversational AI assistant that can understand natural language, answer queries, and help users with everyday tasks using ML models.</div>
      <div class="project-tags">
        <span class="ptag">Python</span>
        <span class="ptag">ML</span>
        <span class="ptag">NLP</span>
      </div>
    </div>

    <div class="project-card reveal">
      <div class="project-icon pi-2"><i class="fas fa-globe"></i></div>
      <i class="fas fa-arrow-up-right-from-square project-arrow"></i>
      <div class="project-title">Smart Society Web App</div>
      <div class="project-desc">A web platform designed to connect communities and automate local governance tasks — from issue reporting to resource allocation for society members.</div>
      <div class="project-tags">
        <span class="ptag">HTML/CSS</span>
        <span class="ptag">JavaScript</span>
        <span class="ptag">Web Dev</span>
      </div>
    </div>

    <div class="project-card reveal">
      <div class="project-icon pi-3"><i class="fas fa-robot"></i></div>
      <i class="fas fa-arrow-up-right-from-square project-arrow"></i>
      <div class="project-title">Automation System</div>
      <div class="project-desc">A Python-based automation framework that handles repetitive digital tasks — file management, data processing, and scheduled workflows with minimal human input.</div>
      <div class="project-tags">
        <span class="ptag">Python</span>
        <span class="ptag">Automation</span>
        <span class="ptag">Scripts</span>
      </div>
    </div>

    <div class="project-card reveal">
      <div class="project-icon pi-4"><i class="fas fa-chart-line"></i></div>
      <i class="fas fa-arrow-up-right-from-square project-arrow"></i>
      <div class="project-title">AI Data Analyzer</div>
      <div class="project-desc">An ML-powered data analysis tool that visualizes patterns, predicts trends, and provides actionable insights from raw datasets using classification and regression models.</div>
      <div class="project-tags">
        <span class="ptag">AI/ML</span>
        <span class="ptag">Data Science</span>
        <span class="ptag">Python</span>
      </div>
    </div>

    <div class="project-card reveal">
      <div class="project-icon pi-5"><i class="fas fa-city"></i></div>
      <i class="fas fa-arrow-up-right-from-square project-arrow"></i>
      <div class="project-title">Smart City Solutions</div>
      <div class="project-desc">Conceptual prototypes exploring IoT, AI, and data-driven approaches for smarter urban living — traffic optimization, waste management, and energy efficiency.</div>
      <div class="project-tags">
        <span class="ptag">IoT</span>
        <span class="ptag">AI</span>
        <span class="ptag">Future Tech</span>
      </div>
    </div>

    <div class="project-card reveal">
      <div class="project-icon pi-6"><i class="fas fa-code"></i></div>
      <i class="fas fa-arrow-up-right-from-square project-arrow"></i>
      <div class="project-title">Web Dev Portfolio Projects</div>
      <div class="project-desc">A collection of responsive, modern web projects built using HTML, CSS, and JavaScript — landing pages, interactive UIs, and real-world frontend implementations.</div>
      <div class="project-tags">
        <span class="ptag">HTML</span>
        <span class="ptag">CSS</span>
        <span class="ptag">JS</span>
      </div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ─── SKILLS ─── -->
<section id="skills">
  <div class="reveal">
    <div class="section-tag">Technical Stack</div>
    <h2 class="section-title">My <span class="gradient-text">Skills</span></h2>
  </div>

  <div class="skills-grid" id="skillsGrid">
    <div class="skill-item reveal">
      <div class="skill-header">
        <div class="skill-name"><div class="skill-icon pi-1"><i class="fab fa-html5"></i></div> HTML5</div>
        <span class="skill-pct">85%</span>
      </div>
      <div class="skill-bar"><div class="skill-fill sf-cyan" data-pct="85"></div></div>
    </div>

    <div class="skill-item reveal">
      <div class="skill-header">
        <div class="skill-name"><div class="skill-icon pi-2"><i class="fab fa-css3-alt"></i></div> CSS3</div>
        <span class="skill-pct">80%</span>
      </div>
      <div class="skill-bar"><div class="skill-fill sf-purple" data-pct="80"></div></div>
    </div>

    <div class="skill-item reveal">
      <div class="skill-header">
        <div class="skill-name"><div class="skill-icon pi-4"><i class="fab fa-js-square"></i></div> JavaScript</div>
        <span class="skill-pct">75%</span>
      </div>
      <div class="skill-bar"><div class="skill-fill sf-orange" data-pct="75"></div></div>
    </div>

    <div class="skill-item reveal">
      <div class="skill-header">
        <div class="skill-name"><div class="skill-icon pi-3"><i class="fab fa-python"></i></div> Python</div>
        <span class="skill-pct">82%</span>
      </div>
      <div class="skill-bar"><div class="skill-fill sf-green" data-pct="82"></div></div>
    </div>

    <div class="skill-item reveal">
      <div class="skill-header">
        <div class="skill-name"><div class="skill-icon pi-6"><i class="fas fa-c"></i></div> C Programming</div>
        <span class="skill-pct">70%</span>
      </div>
      <div class="skill-bar"><div class="skill-fill sf-cyan" data-pct="70"></div></div>
    </div>

    <div class="skill-item reveal">
      <div class="skill-header">
        <div class="skill-name"><div class="skill-icon pi-1"><i class="fas fa-brain"></i></div> AI & ML</div>
        <span class="skill-pct">65%</span>
      </div>
      <div class="skill-bar"><div class="skill-fill sf-purple" data-pct="65"></div></div>
    </div>

    <div class="skill-item reveal">
      <div class="skill-header">
        <div class="skill-name"><div class="skill-icon pi-5"><i class="fab fa-github"></i></div> Git & GitHub</div>
        <span class="skill-pct">78%</span>
      </div>
      <div class="skill-bar"><div class="skill-fill sf-green" data-pct="78"></div></div>
    </div>

    <div class="skill-item reveal">
      <div class="skill-header">
        <div class="skill-name"><div class="skill-icon pi-4"><i class="fas fa-lightbulb"></i></div> Problem Solving</div>
        <span class="skill-pct">88%</span>
      </div>
      <div class="skill-bar"><div class="skill-fill sf-orange" data-pct="88"></div></div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ─── ACHIEVEMENTS ─── -->
<section id="achievements">
  <div class="reveal">
    <div class="section-tag">Aspirations & Goals</div>
    <h2 class="section-title">Achievements & <span class="gradient-text">Vision</span></h2>
  </div>

  <div class="ach-grid">
    <div class="ach-card reveal">
      <div class="ach-icon">🤖</div>
      <div class="ach-title">AI for Society</div>
      <div class="ach-text">Passionate about creating AI-powered technology that genuinely benefits people — from healthcare to education to smart communities.</div>
    </div>

    <div class="ach-card reveal">
      <div class="ach-icon">🎯</div>
      <div class="ach-title">Aspiring AI Engineer</div>
      <div class="ach-text">Goal to become a top-tier AI Engineer and Software Developer, building products that solve complex real-world problems at scale.</div>
    </div>

    <div class="ach-card reveal">
      <div class="ach-icon">💡</div>
      <div class="ach-title">Innovation & Startups</div>
      <div class="ach-text">Deeply interested in the startup ecosystem, innovation culture, and futuristic technology. Aspiring to co-found an AI-first company one day.</div>
    </div>

    <div class="ach-card reveal">
      <div class="ach-icon">🌍</div>
      <div class="ach-title">Real-World Impact</div>
      <div class="ach-text">Building AI products that tackle real-world problems — automation, education access, smart infrastructure, and societal advancement through code.</div>
    </div>
  </div>
</section>

<div class="divider"></div>

<!-- ─── CONTACT ─── -->
<section id="contact">
  <div class="reveal">
    <div class="section-tag">Get In Touch</div>
    <h2 class="section-title">Let's <span class="gradient-text">Connect</span></h2>
  </div>

  <div class="contact-layout">
    <div class="contact-info reveal">
      <h3>Open to collaborations, opportunities &amp; conversations.</h3>
      <p>Whether you're a recruiter, fellow developer, or someone with an exciting idea — I'd love to hear from you. Let's build something amazing together.</p>

      <div class="contact-links">
        <a href="https://github.com/Abhishekbanapur007/my-project" target="_blank" class="contact-link">
          <div class="contact-link-icon cli-github"><i class="fab fa-github"></i></div>
          <div>
            <div style="font-weight:600;font-size:0.88rem;">GitHub</div>
            <div style="font-size:0.75rem;color:var(--text-muted);">@Abhishekbanapur007</div>
          </div>
          <i class="fas fa-arrow-right" style="margin-left:auto;font-size:0.75rem;"></i>
        </a>

        <a href="https://linkedin.com" target="_blank" class="contact-link">
          <div class="contact-link-icon cli-linkedin"><i class="fab fa-linkedin-in"></i></div>
          <div>
            <div style="font-weight:600;font-size:0.88rem;">LinkedIn</div>
            <div style="font-size:0.75rem;color:var(--text-muted);">Abhishek Banapur</div>
          </div>
          <i class="fas fa-arrow-right" style="margin-left:auto;font-size:0.75rem;"></i>
        </a>

        <a href="mailto:abhi464@gmail.com" class="contact-link">
          <div class="contact-link-icon cli-email"><i class="fas fa-envelope"></i></div>
          <div>
            <div style="font-weight:600;font-size:0.88rem;">Email</div>
            <div style="font-size:0.75rem;color:var(--text-muted);">abhi464@gmail.com</div>
          </div>
          <i class="fas fa-arrow-right" style="margin-left:auto;font-size:0.75rem;"></i>
        </a>
      </div>
    </div>

    <div class="contact-form reveal">
      <div class="form-group">
        <label>Your Name</label>
        <input type="text" placeholder="Enter your name...">
      </div>
      <div class="form-group">
        <label>Email Address</label>
        <input type="email" placeholder="Enter your email...">
      </div>
      <div class="form-group">
        <label>Message</label>
        <textarea placeholder="Write your message here..."></textarea>
      </div>
      <button class="btn btn-primary form-submit" onclick="handleSubmit(this)">
        <i class="fas fa-paper-plane"></i> Send Message
      </button>
    </div>
  </div>
</section>

<!-- ─── FOOTER ─── -->
<footer>
  <div class="footer-logo">AB.</div>
  <p class="footer-text">
    © 2026 Abhishek Banapur &nbsp;|&nbsp; Built with <span class="footer-heart">♥</span> Passion and Innovation
  </p>
</footer>

<script>
// ─── CANVAS PARTICLES ───
const canvas = document.getElementById('canvas-bg');
const ctx = canvas.getContext('2d');
let particles = [];

function resize() {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
}

function initParticles() {
  particles = [];
  const count = Math.floor((canvas.width * canvas.height) / 12000);
  for (let i = 0; i < count; i++) {
    particles.push({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      r: Math.random() * 1.5 + 0.3,
      vx: (Math.random() - 0.5) * 0.3,
      vy: (Math.random() - 0.5) * 0.3,
      o: Math.random() * 0.5 + 0.1,
      c: Math.random() > 0.5 ? '0,212,255' : '124,58,255'
    });
  }
}

function drawParticles() {
  ctx.clearRect(0, 0, canvas.width, canvas.height);
  particles.forEach(p => {
    p.x += p.vx;
    p.y += p.vy;
    if (p.x < 0) p.x = canvas.width;
    if (p.x > canvas.width) p.x = 0;
    if (p.y < 0) p.y = canvas.height;
    if (p.y > canvas.height) p.y = 0;
    ctx.beginPath();
    ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
    ctx.fillStyle = `rgba(${p.c},${p.o})`;
    ctx.fill();
  });

  // Draw connections
  for (let i = 0; i < particles.length; i++) {
    for (let j = i + 1; j < particles.length; j++) {
      const dx = particles[i].x - particles[j].x;
      const dy = particles[i].y - particles[j].y;
      const dist = Math.sqrt(dx * dx + dy * dy);
      if (dist < 100) {
        ctx.beginPath();
        ctx.moveTo(particles[i].x, particles[i].y);
        ctx.lineTo(particles[j].x, particles[j].y);
        ctx.strokeStyle = `rgba(0,212,255,${0.06 * (1 - dist / 100)})`;
        ctx.lineWidth = 0.5;
        ctx.stroke();
      }
    }
  }

  requestAnimationFrame(drawParticles);
}

window.addEventListener('resize', () => { resize(); initParticles(); });
resize();
initParticles();
drawParticles();

// ─── SCROLL REVEAL ───
const revealEls = document.querySelectorAll('.reveal');
const observer = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      e.target.classList.add('visible');
      // Animate skill bars
      const fills = e.target.querySelectorAll('.skill-fill');
      fills.forEach(f => {
        setTimeout(() => { f.style.width = f.dataset.pct + '%'; }, 200);
      });
    }
  });
}, { threshold: 0.12 });

revealEls.forEach(el => observer.observe(el));

// Trigger skill bars that are already visible
const allFills = document.querySelectorAll('.skill-fill');
const skillObserver = new IntersectionObserver((entries) => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      const fill = e.target;
      setTimeout(() => { fill.style.width = fill.dataset.pct + '%'; }, 300);
      skillObserver.unobserve(e.target);
    }
  });
}, { threshold: 0.5 });

allFills.forEach(f => skillObserver.observe(f));

// ─── NAV SCROLL ───
window.addEventListener('scroll', () => {
  const nav = document.getElementById('navbar');
  nav.style.background = window.scrollY > 50
    ? 'rgba(5,10,18,0.92)'
    : 'rgba(5,10,18,0.7)';
});

// ─── MOBILE MENU ───
function toggleMenu() {
  document.getElementById('mobileMenu').classList.toggle('open');
}

// ─── FORM SUBMIT ───
function handleSubmit(btn) {
  btn.innerHTML = '<i class="fas fa-check"></i> Message Sent!';
  btn.style.background = 'linear-gradient(135deg, #00ffb3, #00d4ff)';
  btn.disabled = true;
  setTimeout(() => {
    btn.innerHTML = '<i class="fas fa-paper-plane"></i> Send Message';
    btn.style.background = '';
    btn.disabled = false;
  }, 3000);
}
</script>
</body>
</html>
